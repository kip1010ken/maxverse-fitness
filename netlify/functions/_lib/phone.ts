// General contact-form phone: optional +, 9-15 digits.
export const CONTACT_PHONE_PATTERN = /^\+?\d{9,15}$/;

// Daraja MSISDN format: 2547XXXXXXXX (no plus, no leading zero).
export const DARAJA_PHONE_PATTERN = /^2547\d{8}$/;
